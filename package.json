{
  "name": "smn-app-v1",
  "version": "0.1.0",
  "private": true,
  "description": "Bootstrap monorepo for cross-platform applications.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "install": "node ./scripts/install.mjs",
    "lint": "node ./scripts/lint.mjs",
    "test": "node ./scripts/test.mjs",
    "dev": "node ./scripts/dev.mjs"
  }
}
